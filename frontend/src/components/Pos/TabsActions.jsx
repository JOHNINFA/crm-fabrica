import React from "react";

export default function TabsActions({ selectedTab, setSelectedTab }) {
  
  return (
    <div className="d-flex align-items-center gap-2 mb-1 flex-wrap">
      {/* Botón movido al Topbar */}
    </div>
  );
}